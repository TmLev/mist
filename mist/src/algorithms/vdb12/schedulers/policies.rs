use crate::vdb12::Application;

////////////////////////////////////////////////////////////////////////////////////////////////////

pub trait UnfeasiblePolicy {
    fn handle(&self, application: &mut Application);
}

////////////////////////////////////////////////////////////////////////////////////////////////////

pub struct UnfeasibleToPublic {}

impl UnfeasiblePolicy for UnfeasibleToPublic {
    fn handle(&self, application: &mut Application) {}
}

////////////////////////////////////////////////////////////////////////////////////////////////////

pub struct CheapestToPublic {}

impl UnfeasiblePolicy for CheapestToPublic {
    fn handle(&self, application: &mut Application) {}
}
